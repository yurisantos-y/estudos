/* const pontuacao = 1000;
const nivelUsuario = pontuacao >= 1000 ? 'Usuário Vip' : 'Usuário Normal';

console.log(nivelUsuario);*/

const resultado = 2000;
const pontuacao = resultado >= 1000 ? 'Passou' : 'Não Passou';

console.log(pontuacao);