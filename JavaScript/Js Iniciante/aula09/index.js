const nome1 = "Yuri"; // String
const nome2 = 'Yuri'; // String
const nome3 = `Yuri`; // String
const num1 = 123; // Number
const num2 = 123.15; // Number com ponto flutuante
let nomeAluno; // Undefined -> não aponta para nenhum local na memória. Foi utlizado let pois const precisa inicializar a variavel
const sobrenomeAluno = null; // Nulo -> não aponta para nenhum local na memória
const aprovado = false; // Boolean = true, false (Booelena é um valor lógico)

/* Incremento ou Decremento */
const passo = 50;
let contador = 0;
contador += passo; // A operação a esquerda é igual a: contador = contador + 50

let passoMulti = 50;
 passoMulti *= 10;
console.log(contadorMulti);

